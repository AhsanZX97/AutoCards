User-agent: *
Allow: /$
Allow: /privacy
Allow: /terms

# Nothing under /app is useful to a crawler — it all requires an account, and
# every one of these URLs renders the same sign-in redirect.
Disallow: /app
Disallow: /sign-in
Disallow: /sign-up
Disallow: /forgot-password
Disallow: /reset-password
